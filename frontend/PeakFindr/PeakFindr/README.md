
# Peakfindr iOS (SwiftUI + MVVM) — Backend Integrated v2

Updates requested:
- Location endpoints are now public (no X-User-ID).
- Swipe right **saves** the location and opens details.
- Added Saved tab listing user's saved locations.
- Saved detail shows **Check In (+points)** and Write Review.
- Removed streak UI/functionality from Profile.

## Setup
1. Create a new Xcode SwiftUI iOS project named `Peakfindr`
2. Drag all files from this zip into Xcode (Copy items if needed).
3. Set baseURL in `Services/APIClient.swift`.

